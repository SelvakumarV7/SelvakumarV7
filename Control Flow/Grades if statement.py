#An if statement is used to test a condition for truth.
#Create a grades.py program that checks whether a grade is above or below 55.

import random
grade = random.randint(40,90)
if grade >=55:
    print("Pass")
else:
    print("Fail")
